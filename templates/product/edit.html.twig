{% extends 'base.html.twig' %}

{% block title %}{{ 'product.edit_title'|trans({}, "pages") }}{% endblock %}

{% block body %}
<div class="container">
    <h1>{{ 'product.edit_title'|trans({}, "pages") }}</h1>

    {{ include('product/_form.html.twig') }}
    <hr>
    <div class="row">
        <div class="col-auto">
            <a type="button" class="btn btn-primary" href="{{ path('product_index') }}">{{ 'buttons.back_list'|trans({}, "pages") }}</a>
        </div>
        <div class="col-auto">
            {{ include('product/_delete_form.html.twig') }}
        </div>
    </div>

</div>
{% endblock %}
